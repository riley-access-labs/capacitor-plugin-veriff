import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'capacitor-plugin-veriff-sample',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
